# 🚀 دليل تشغيل وتجربة منصة الـ ERP والمتجر الإلكتروني (Enterprise ERP & Store Manual)

مرحباً بك! هذا الدليل الشامل يوضح لك كيفية تجربة جميع مزايا وأنظمة المتجر الإلكتروني ونظام إدارة موارد المؤسسة (ERP Platform)، مع حسابات الدخول المجهزة وبيانات التجربة لكل دور.

---

## 🔑 1. بيانات حسابات الدخول للتجربة (Test Credentials)

يمكنك تسجيل الدخول عبر الصفحة `/login` باستخدام الحسابات التالية وفقاً للدور الذي ترغب بتجربته:

### 1️⃣ حساب مدير النظام العام (Admin / Super Administrator)
* **البريد الإلكتروني**: `admin@store.com`
* **كلمة المرور**: `admin123`
* **الصلاحيات**: وصول كامل وغير محدود إلى لوحة التحكم الإدارية الـ ERP، المحاسبة، المشتريات، المبيعات، المخزون، الشجرة التنظيمية، والتقارير.

---

### 2️⃣ حساب الموظف (Employee)
* **البريد الإلكتروني**: `employee@store.com`
* **كلمة المرور**: `emp123`
* **الصلاحيات**: متابعة العمليات والمهام الإدارية والطلبات الموكلة.

---

### 3️⃣ حسابات العملاء والمتسوقين (Customers)
* **البريد الإلكتروني الرئيسي**: `mohammed@gmail.com`
* **كلمة المرور**: `mm123mm`
* **حسابات عملاء إضافية (تستخدم نفس كلمة المرور `mm123mm`)**:
  - `fatima@gmail.com` (فاطمة السالم)
  - `khaled@gmail.com` (خالد الراشد)
  - `sara@gmail.com` (سارة الشمري)
* **الصلاحيات**: تصفح المتجر، إضافة المنتجات للسلة، الشراء ومتابعة الطلبات.

---

## 🛒 2. خطة تجربة واجهة المتجر الإلكتروني (Storefront Walkthrough)

| الصفحة / الميزة | المسار (URL) | كيفية التجربة والتحقق |
| :--- | :--- | :--- |
| **الصفحة الرئيسية** | `/` | استعراض العرض البصري المتقدم، المنتجات المميزة، الألوان والوضعين الداكن والفاتح. |
| **قائمة المنتجات والفلترة** | `/products` | تجربة الفلترة حسب الفئة والبحث وعرض تفاصيل كل منتج. |
| **السلة ثلاثية الأبعاد 3D** | في جميع الصفحات | عند النقر على `إضافة للسلة`: ظهور إشعار أعلى الشاشة وتضخم الأيقونة العائمة **3D Floating Cart Orb** دون فتح السلة تلقائياً. |
| **سلة الطلبات الجانبية** | زر السلة العائم | النقر على السلة العائمة لفتح القائمة الجانبية وتجربة الزرين: `متابعة التسوق` و `إتمام الطلب والدفع ➔` مع إجمالي المشتريات. |
| **صفحة الدفع والشراء** | `/checkout` | ملء بيانات التوصيل وإتمام الطلب لتوليد أمر مبيعات جديد تلقائياً. |
| **حساب العميل والطلبات** | `/account` و `/orders` | استعراض قائمة طلبات العميل السابقة وعناوين الشحن المسجلة. |

---

## 🏛️ 3. خطة تجربة لوحة تحكم إدارة المؤسسة (Admin ERP Dashboard)

قم بتسجيل الدخول بحساب المدير (`admin@store.com` / `admin123`) ثم انتقل إلى `/admin`:

### 1. إدارة الطلبات والمبيعات (O2C) — `/admin/orders`
- متابعة الطلبات الحالية وتغيير حالاتها (`PENDING` -> `CONFIRMED` -> `SHIPPED` -> `DELIVERED`).
- طباعة الفواتير الضريبية المباشرة والمجهزة برمز الـ QR الخاص بهيئة الزكاة والضريبة والجمارك (ZATCA).

### 2. إدارة المشتريات والموردين (P2P) — `/admin/purchases`
- استعراض أوامر الشراء الموجهة للموردين.
- النقر على زر **استلام البضاعة والتوريد** لملاحظة الزيادة التلقائية للمخزون وتسجيل القيد المحاسبي.

### 3. إدارة المخزون والمستودعات — `/admin/inventory`
- استعراض حركات المخزون المباشرة (`SALE`, `PURCHASE`, `RETURN`, `ADJUSTMENT`).
- إجراء تعديل جردي لملاحظة تحديث الكميات ذرّياً دون حدوث أي مخزون بالسالب.

### 4. المحاسبة والشؤون المالية (Financial Engine) — `/admin/accounting`
- **دليل الحسابات (COA)**: استعراض شجرة الحسابات (الأصول، الالتزامات، حقوق الملكية، الإيرادات، والمصروفات).
- **قيود اليومية المزدوجة**: استعراض القيود المتوازنة تلقائياً ($\sum \text{Debit} = \sum \text{Credit}$).
- **القوائم المالية**: استعراض ومطابقة **ميزان المراجعة**، **قائمة الدخل**، و**الميزانية العمومية** مع طباعة التقرير الشامل.

### 5. الهيكل التنظيمي والتفويض (RBAC & Governance) — `/admin/roles`
- استعراض مصفوفة الصلاحيات الدقيقة والأقسام.
- استعراض الشجرة الهيكلية للتسلسل الإداري (Visual Org Chart Tree).
- متابعة التفويضات المؤقتة النشطة للمسؤولين.

### 6. التقارير وذكاء الأعمال (BI & Documents) — `/admin/reports`
- استعراض التحليلات الرسمية للفترات (اليوم، الأسبوع، الشهر، السنة).
- النقر على زر الطباعة لمعاينة وتصدير التقارير بصيغ PDF و CSV.

### 7. مركز الإشعارات والأتمتة — `/admin/activity`
- متابعة التنبيهات الحية (نقص المخزون، أذونات الشراء).
- استعراض طوابير المهام المؤجلة ومحرك قواعد الأعمال.

### 8. صحة النظام وسجلات التدقيق (SRE & Operations) — `/admin/audit-logs`
- استعراض سجلات أمان وتدقيق العمليات.
- فتح تبويب **مراقبة صحة الخادم** لمتابعة زمن استجابة قاعدة البيانات (`3ms`) والـ APIs وتفاصيل الاستهلاك.

### 9. إعدادات المتجر العامة — `/admin/settings`
- تعديل اسم المتجر، الشعار، نسبة الضريبة (15%)، العملة (ر.س)، والرموز للتحقق من تحديثها التلقائي عبر كامل موديولات النظام.

---

## 💻 4. تشغيل المشروع محلياً (Running Locally)

لتشغيل السيرفر المحلي للتطوير:
```bash
npm run dev
```

لإجراء اختبار البناء الإنتاجي (Production Build):
```bash
npm run build
```

---
**جاهز للتجربة الكاملة والتشغيل بنسبة 100%!**
